# Python Markdown 教程书：第5章

本包是第5章《面向对象程序设计》的 Markdown 教材交付。

## 内容

- `chapters/`：教材正文
- `assets/ch05/`：本章正式图片，全部本地化、居中显示、图注在下
- `assets/ch05/web/`：原始网络素材、真实运行图和脚本生成预览图
- `code/ch05/`：可运行示例代码
- `output/`：脚本生成的 JSON 和预览图
- `reports/`：脚本生成的对象模型报告、类职责卡片、对象协作消息图、对象质量回执和对象交付包报告
- `scripts/check_links.py`：Markdown 与 HTML 图片链接检查
- `scripts/generate_ch05_visuals.py`：本章结构化图片生成脚本
- `source_notes/`：来源记录和质量验收
- `manifest.json`：交付清单

## 本章项目

学习卡片对象模型：用类封装学习卡片、卡片盒和实验试次，让代码从散装变量变成可维护模型，并生成对象模型报告、类职责卡片、对象协作消息图、对象质量回执、可复用对象交付包和 OOP 运行证据清单。

## 本轮优化重点

- 正文重写为对标 ch01/ch04 的结构：`[TOC]`、本章导读、分区导航、五个学习部分、运行证据、练习、自测和复盘模板。
- 正文当前引用 26 张有教学用途的正式图，历史照片、认知心理学素材和代码文化梗图重新穿插在对应知识点旁边，不再集中堆图。
- `scripts/generate_ch05_visuals.py` 改为结构化绘图脚本，重新生成封面、路线图、最小类对照图、OOP 心智模型、心理学试次对象、常见坑地图和项目交付链。
- 修复脚本生成图里的机械英文和机翻感：对象协作消息图、对象质量回执、对象交付包、GUI 面板对象模型和运行证据都改为自然中文标签。
- `04_make_oop_model_report.py` 与 `05_make_design_cards.py` 增加 web 资产同步，保证正文正式图能稳定引用最新脚本产物。
- 后半段证据链保持完整：对象模型报告、职责卡片、协作图、质量回执、交付包 JSON、GUI 面板对象模型和运行证据均可重新生成。
- 图片排版经接触表与满尺寸关键图检查，重点确认中文没有越界、步骤图含义明确、图注与正文对应。
